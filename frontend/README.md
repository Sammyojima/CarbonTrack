# CarbonTrack Frontend (Vite + React + Tailwind)

## Quick start

1. Install dependencies:
```
npm install
```

2. Start dev server:
```
npm run dev
```

3. The app expects a backend API at the configured base URL in `src/services/api.js`.
